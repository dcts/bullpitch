let wSize = 400;

function setup() {
  let myCanvas = createCanvas(wSize, wSize);


}

function draw() {
  background(50);
  // check for collisions

  ellipse(wSize/2.0, wSize/2.0, wSize/2.0, wSize/2.0);

}

function keyPressed() {
  if (key=='a') {
  }
  if (key=='d') {
  }
  if (key==' ') {
  }
}

function keyReleased() {
  if (key=='a') {
  }
  if (key=='d') {
  }
}
